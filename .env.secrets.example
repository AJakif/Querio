# Optional secret overrides. Keep the real file untracked.
# Copy this file to `.env.secrets` for local runs, or mount a secrets file
# and point `QUERIO_SECRETS_FILE` at it in Docker.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
